import { ServerModel } from "../model/ServerModel";

export class ServerFacade {

    //Will add these once the model and command classes exist

    // private model: ServerModel;

    constructor() {
    //     this.model = new ServerModel();
    }

    // createGame(name): Game {
    //     return;
    // }

    // getGameList(): Game[] {
    //     return;
    // }

    // register(name, password): RegisterCommand {
    //     return;
    // }
    
    // login(name, password): LoginCommand {
    //     return;
    // }
}