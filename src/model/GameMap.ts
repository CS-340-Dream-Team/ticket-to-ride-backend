import { Segment } from "./Segment";
import { Location } from "./Location";

export class GameMap{
    //segments:Segment[]
    //locations:Location[]
    constructor(){

        //this.segments=segments
        //this.locations=locations
    }
    claim(segmentID:number, playerName:string){
        //this.segments[segmentID].claim(playerName)
    }
}